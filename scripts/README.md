# Scripts

## sync-projects.js

Syncs project data from GitHub using the `gh` CLI.

```bash
npm run sync
```

Fetches repos from whaleen, nothingdao, and orthfx orgs and writes `_data/projects.json`.
